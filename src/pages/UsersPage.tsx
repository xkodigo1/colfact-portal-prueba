import { Card } from '@/components/ui/Card';

export const UsersPage = () => {
  return (
    <Card>
      <h1 className="text-3xl font-bold text-surface-900">Módulo de usuarios</h1>
      <p className="mt-3 text-sm text-surface-700">
        La base del proyecto ya contempla la ruta donde se montará la administración de usuarios.
      </p>
    </Card>
  );
};
